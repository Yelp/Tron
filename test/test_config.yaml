--- !TronConfiguration

nodes:
    - &nodeBatch00 !Node
        hostname: bastion.yelpcorp.com

jobs:
    - &jobFoo !Job
        name: "foo"
        node: *nodeBatch00
        command: "true"
        schedule: !IntervalScheduler
            interval: 10s