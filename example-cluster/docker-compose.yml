version: "3"
services:
  master:
    build:
      context: ../
      dockerfile: yelp_package/trusty/Dockerfile
    volumes:
      - ../:/work
      - ./:/nail/tron
    links:
      - batch1
      - batch2
  batch1:
    build:
      context: images/batch
  batch2:
    build:
      context: images/batch
